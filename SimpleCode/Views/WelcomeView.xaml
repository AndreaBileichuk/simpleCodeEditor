<UserControl x:Class="SimpleCode.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:SimpleCode.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100">
    <UserControl.Resources>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#F4D9D0" />
            <Setter Property="Foreground" Value="#921A40" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0 0 0 10" />
            <Setter Property="Width" Value="240" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="BorderBrush" Value="#C75B7A" />
            <Setter Property="BorderThickness" Value="1.5" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D9ABAB" />
                                <Setter Property="BorderBrush" Value="#921A40" />
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#921A40" BlurRadius="8" Opacity="0.3" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.03" ScaleY="1.03" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C75B7A" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Foreground" Value="#921A40" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,10,0,10" />
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#F4D9D0" Offset="0" />
                <GradientStop Color="#D9ABAB" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Padding="0 150 0 0" Grid.Row="0">Welcome to the world of code with the SimpleCode editor</TextBlock>
        <TextBlock Grid.Row="1" Margin="0 10 0 30">To start, choose an option below</TextBlock>

        <Button Grid.Row="2" Command="{Binding OpenFolderCommand}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <fa:ImageAwesome Icon="FolderOpen" Width="24" Height="24" Margin="0,0,8,0" Foreground="#921A40" />
                <TextBlock Text="Open Folder" VerticalAlignment="Center" />
            </StackPanel>
        </Button>

        <Button Grid.Row="3" Command="{Binding CreateFolderCommand}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <fa:ImageAwesome Icon="FolderOpen" Width="24" Height="24" Margin="0,0,8,0" Foreground="#921A40" />
                <fa:ImageAwesome Icon="Plus" Width="14" Height="14" Margin="-12,6,6,0" Foreground="#921A40" />
                <TextBlock Text="Create Root Folder" VerticalAlignment="Center" />
            </StackPanel>
        </Button>

        <Button Grid.Row="4" Command="{Binding CloseProgramCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <fa:ImageAwesome Icon="Close" Width="20" Height="20" Margin="0,0,8,0" Foreground="#921A40" />
                <TextBlock Text="Close the program" VerticalAlignment="Center" />
            </StackPanel>
        </Button>

    </Grid>
</UserControl>